#define _CRT_SECURE_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS_GLOBALS

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

char taper_mot(char *mot)
{
	scanf("%s", mot);
	return mot;
}

bool palindrome(char *mot, size_t max)
{
	return true;
}

int main(int argc, char *argv[])
{
	char mot[100]="radar";
	printf("%s\n", mot);
	
	return 0;
}